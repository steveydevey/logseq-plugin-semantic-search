{
  "name": "logseq-plugin-semantic-search",
  "version": "0.1.0",
  "description": "Semantic search across all Logseq blocks using text embeddings",
  "main": "dist/index.html",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@logseq/libs": "^0.0.17"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "logseq": {
    "id": "logseq-plugin-semantic-search",
    "title": "Semantic Search",
    "icon": "./icon.svg"
  }
}
